import { AuthService } from './auth.service';

describe('AuthService', () => {
  describe('#Authenticate', () => {
    test.todo('should not authenticate if user does not exist');
    test.todo('should not authenticate passwords noes not match');
    test.todo('should authenticate with valid credentials');
  });
});
